# Kubios Screenshot Fixtures

`synthetic/` is generated locally and contains no real measurements or private
screen content. `anonymized_real/` contains only user-approved, cropped samples
without identity or notification content. Each accepted fixture has a reviewed
sibling `.ground_truth.json` file and remains offline.

Synthetic metrics must never be reported as real-world recognition accuracy.
